passdb {
  driver = passwd-file
  args = /data/initial-configs/passwd
}
userdb {
  driver = passwd-file
  args = username_format=%n /data/initial-configs/passwd
  default_fields = uid=nobody gid=nogroup home=/var/vmail/%u
}
